import * as React from 'react'
import Loader from '../loader'
import UI from '../ui'
import Previewer from '../previewer'

export default () => (
  <div>
    <Loader /> <Previewer /> <UI />
  </div>
)
